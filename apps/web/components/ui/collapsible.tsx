"use client"

import { Collapsible as BaseCollapsible } from "@base-ui/react/collapsible"

const Root = BaseCollapsible.Root

const Trigger = BaseCollapsible.Trigger

const Panel = BaseCollapsible.Panel

export const Collapsible = {
  Root,
  Trigger,
  Panel,
}
